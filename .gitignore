# .gitignore for the Cookiecutter Template Repository

# --- Virtual Environment ---
# Ignore local virtual environments used for developing the template
venv/
.venv/
env/
.env/                # For dotenv files if used in template development
pipenv.log           # If pipenv is used for template development
.python-version      # pyenv local version file

# --- Python Build / Cache / Metadata ---
__pycache__/         # Python bytecode cache
*.pyc                # Compiled Python files
*.egg-info/          # Python egg info directories
.pytest_cache/       # Pytest cache directory
.mypy_cache/         # MyPy type checking cache

# --- Test / Coverage Reports ---
.coverage            # Coverage.py data file
htmlcov/             # HTML coverage report directory

# --- IDEs and Editors ---
.idea/               # JetBrains IDEs (e.g., PyCharm)
.vscode/             # VS Code settings and cache
*.sublime-project
*.sublime-workspace
*.swp                # Vim swap files

# --- OS generated files ---
.DS_Store            # macOS directory metadata
Thumbs.db            # Windows thumbnail cache

# --- Cookiecutter Specifics (for template development) ---
# Files generated by Cookiecutter during template testing
.cookiecutter_replay/ # Stores prompt answers for replay
cookiecutter.json.tmp # Temporary file during cookiecutter runs
